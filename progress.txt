# GRMRL – Progress Log – Iterazione 6

## Codebase Patterns
- Alpine.js 3.15 installed via npm; initialized in `resources/js/app.js` with `import Alpine; window.Alpine = Alpine; Alpine.start()`
- `public/build` is gitignored — run `npm run build` on the host before testing UI (node is available outside Docker)
- Use `docker-compose exec app <command>` to run artisan/composer commands inside the container
- Filament requires `ext-intl` — add `libicu-dev` + `docker-php-ext-configure intl` + `docker-php-ext-install intl` to Dockerfile
- Filament 3.3 requires `symfony/console ^6|^7` but Laravel 13 locks it to v8. Install with `composer require filament/filament:'^3.3' --with-all-dependencies` to allow the downgrade to v7.4
- The User model must implement `Filament\Models\Contracts\FilamentUser` and define `canAccessPanel(Panel $panel): bool`
- `php artisan filament:install --panels` emits a harmless `xdg-open: not found` error in Docker (no browser) — installation succeeds anyway
- `/admin` returns 302 (redirect to login); follow with `-L` to get the 200 from the login page
- Migration additive (non distruttiva): usare `Schema::table()` con `after('column')` per aggiungere campi senza perdere dati
- `db:seed --class=ActivitySeeder` ricrea le attività; in produzione usare `--force`
- Nominatim geocoding: User-Agent obbligatorio, pausa 1s tra richieste, query con "+ Italia" per precisione

---

## 2026-06-27 - US-045
- Rimossa validazione `required`/`min:1` per `minors` in `RegistrationController::store()` → ora `nullable|array|max:3`
- Rimosse chiavi `minors.required` e `minors.min` dall'array `$messages`
- Rimosso blocco `@error('minors')` con messaggio "È obbligatorio aggiungere almeno un minore." dalla view `form.blade.php`
- Rimosso `@error('minors') border-red-400 bg-red-50 @enderror` dalla classe CSS del div card minori
- **Learnings for future iterations:**
  - La validazione `minors.*.field` rimane intatta: se si aggiunge un minore, i suoi campi restano obbligatori
  - Il messaggio di testo statico nella view era duplicato rispetto ai messaggi di validazione Laravel (rimozione pulita)
---

## 2026-06-27 - US-046
- Sostituito il testo del consenso `rules_accepted` in `resources/views/registration/form.blade.php`
- "Ho letto la locandina" → "Ho consultato le <a ...>informazioni sull'attività</a>" con link a https://organizzazione.cai.it/gr-lombardia/progetti-regione-lom/attivita/
- Link con `target="_blank" rel="noopener noreferrer"` e stile `text-blue-600 underline hover:text-blue-800`
- L'asterisco obbligatorio rimane invariato
- **Learnings for future iterations:**
  - Modifica puntuale in `form.blade.php` attorno alla riga 478 (sezione consenso rules_accepted)
  - Nessun cambio PHP richiesto: solo Blade template
---
